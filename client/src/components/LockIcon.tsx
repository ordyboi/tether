import { Ionicons } from "@expo/vector-icons";
import { colors } from "@/utils/theme";

export function LockIcon() {
  return <Ionicons name="lock-closed-outline" size={16} color={colors.primary} />;
}
