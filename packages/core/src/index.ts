// Shared platform-agnostic core (crypto, protocol, domain types) — see
// docs/ARCHITECTURE.md §6.1. Populated from Phase 1.2 onward; empty skeleton
// during Phase 0 so apps/web can depend on the package path from day one.
export {};
