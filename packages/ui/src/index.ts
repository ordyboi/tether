// Design-system home (tokens + primitives) — see docs/DELIVERY_PLAN.md 0.2.
// Deliberately empty until Phase 0's three design directions (apps/web/app/1,
// /2, /3) are compared and one is chosen; this package is where the winner
// gets extracted and refined into reusable primitives.
export {};
